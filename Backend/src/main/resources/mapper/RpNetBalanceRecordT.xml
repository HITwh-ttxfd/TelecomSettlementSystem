<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.backend.mapper.RpNetBalanceRecordTMapper">

    <resultMap type="RpNetBalanceRecordT" id="Search">
        <result property="ID" jdbcType="int" column="ID"/>
        <association property="location" javaType="cn.com.dhee.entity.Locations"> <id property="locationId" jdbcType="INTEGER" column="location_id"/>
            <result property="cityName" jdbcType="CHAR" column="street_address"/>
        </association>

    </resultMap>
    <!-- 查询城市编码和城市名称 -->
    <select id="selectAllRpNetBalanceRecordT"   resultMap="Search" >
        select ID,CITY_NAME
        from rp_net_balance_record_t natural join rp_city_code_t
        <!--        <where>-->
        <!--            &lt;!&ndash; 对于字符串来说既要判断null，也要判断空字符串 &ndash;&gt;-->
        <!--            &lt;!&ndash; 在test属性中和＃{}中使用的是实体类（参数）中的数据域，不在这里的指的是表中的列名。 &ndash;&gt;-->
        <!--            <if test="cityCode != null and cityCode != '' ">-->
        <!--                AND city_code=#{cityCode}-->
        <!--            </if>-->
        <!--            <if test="cityName != null and cityCode != '' ">-->
        <!--                AND city_Name=#{cityName}-->
        <!--            </if>-->

        <!--        </where>-->
    </select>

</mapper>
